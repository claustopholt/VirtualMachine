# Example source code to be compiled for the VirtualMachine.

script
{
    a = (5 > 1 + 1);

    # Add some values to two variables.
    #a = 2;
    #b = 10 - (8 / 2);

    #if (a == 2)
    #{
        #output(a + b);
    #}

    #output(123);

    #while a < 20
    #{
        #a = a + (b * 2) - 3
        #print(a)
    #}
}
