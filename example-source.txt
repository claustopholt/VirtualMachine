# Example source code to be compiled for the VirtualMachine.

script
{
    a = 2;
    b = 10 - (8 / 2);

    output(a + b);

    #while a < 20
    #{
        #a = a + (b * 2) - 3
        #print(a)
    #}
}
